#include <stdio.h> 
int main(){
	int a;
	scanf("%d",&a);
	printf("min = a if( a < min );\nr0 = - b*c - sqrt( 3*a - c*c/17 ) if( 4*a*c - b*b < 32*c + 9 );\n c /= 2 if( c+3*a < log(9*b) );\nperl = smart if( 1 == 2 );\n");
	return 0;
}
